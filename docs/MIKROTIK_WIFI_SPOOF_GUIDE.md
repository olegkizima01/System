# MikroTik WiFi & MAC Spoofing - Посібник використання

## 📋 Огляд

Модуль `mikrotik_wifi_spoof.sh` призначений для автоматичного спуфінгу:
- **WiFi SSID** на MikroTik роутері
- **IP підмережі** для гостьової мережі  
- **MAC адреси** локального інтерфейсу

## 🔧 Конфігурація

### Змінні середовища (в `.env`):
```bash
SUDO_PASSWORD=your_password          # Пароль для sudo команд
MIKROTIK_HOST=192.168.88.1         # IP адреса MikroTik
MIKROTIK_USER=admin                 # Користувач MikroTik
SSH_KEY=~/.ssh/id_ed25519           # SSH ключ для доступу
```

### Налаштування мереж:
```bash
SOLO5_SSID="Solo5"                  # Резервна мережа для доступу до MikroTik
SOLO5_PASSWORD=""                   # Порожній якщо мережа збережена
GUEST_WIFI_PASSWORD="00000000"     # Пароль гостьової мережі
```

## 🚀 Використання

### Основні команди:

```bash
# Тестування доступу до MikroTik
./scripts/cleanup/mikrotik_wifi_spoof.sh test

# Переключитися на мережу Solo5
./scripts/cleanup/mikrotik_wifi_spoof.sh switch-solo5

# Повний спуфінг з автоматичним перепідключенням
./scripts/cleanup/mikrotik_wifi_spoof.sh spoof-auto

# Лише спуфінг без перепідключення
./scripts/cleanup/mikrotik_wifi_spoof.sh spoof

# Перевірка статусу
./scripts/cleanup/mikrotik_wifi_spoof.sh status

# Допомога
./scripts/cleanup/mikrotik_wifi_spoof.sh help
```

## 🔄 Логіка роботи

### 1. Автоматичне переключення на Solo5
Якщо MikroTik недоступний з поточної мережі, скрипт автоматично:
- Відключається від поточної мережі
- Підключається до мережі Solo5
- Повторно перевіряє доступ до MikroTik

### 2. Локальний спуфінг
Якщо MikroTik недоступний навіть через Solo5:
- Виконується лише локальна зміна MAC адреси
- Генерується новий MAC для інтерфейсу
- Логується спроба зміни

### 3. Повний спуфінг
При успішному доступі до MikroTik:
- Генерується новий SSID: `Guest_XXXXXX`
- Генерується нова підмережа: `10.XXX.XXX.0/24`
- Генерується нова MAC адреса: `02:XX:XX:XX:XX:XX`
- Оновлюється конфігурація MikroTik
- Змінюється локальна MAC адреса

## 🛠️ Налаштування MikroTik

### SSH доступ:
```bash
# Додати SSH ключ до MikroTik
ssh-copy-id -i ~/.ssh/id_ed25519 admin@192.168.88.1

# Або вручну на MikroTik:
/user ssh-keys import user=admin public-key-file="id_ed25519.pub"
```

### WiFi інтерфейси:
Скрипт очікує гостьові WiFi інтерфейси:
- `wifi3` - перший гостьовий інтерфейс
- `wifi4` - другий гостьовий інтерфейс

## 🔍 Діагностика

### Перевірка доступу:
```bash
# Тест з поточної мережі
./scripts/cleanup/mikrotik_wifi_spoof.sh test

# Перевірка SSH з'єднання
ssh -i ~/.ssh/id_ed25519 admin@192.168.88.1 "system identity print"
```

### Логи:
```bash
# Логи спроб зміни MAC
cat /tmp/mac_spoof_attempts.log

# Логи роботи скрипту
ls /tmp/mikrotik_wifi_spoof_*.log
```

## ⚠️ Важливі примітки

1. **Solo5 мережа** - повинна бути збережена в системі для автоматичного підключення
2. **MAC спуфінг** - може не працювати на всіх інтерфейсах macOS
3. **SSH ключ** - повинен бути доданий до MikroTik заздалегідь
4. **Права доступу** - потрібен sudo пароль для зміни мережевих налаштувань

## 📝 Приклади використання

### Базовий тест:
```bash
export SUDO_PASSWORD="your_password"
./scripts/cleanup/mikrotik_wifi_spoof.sh test
```

### Повний цикл:
```bash
export SUDO_PASSWORD="your_password"
./scripts/cleanup/mikrotik_wifi_spoof.sh spoof-auto
```

### Ручне керування:
```bash
# Переключитись на Solo5
./scripts/cleanup/mikrotik_wifi_spoof.sh switch-solo5

# Перевірити статус
./scripts/cleanup/mikrotik_wifi_spoof.sh status

# Виконати спуфінг
./scripts/cleanup/mikrotik_wifi_spoof.sh spoof
```

## 🔧 Інтеграція з Windsurf

Модуль автоматично інтегрований в систему cleanup для Windsurf:
- Включений в `cleanup_modules.json`
- Виконується останнім (order: 999)
- Перевизначає попередні налаштування мережі
