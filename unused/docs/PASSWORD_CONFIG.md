# 🔐 Конфігурація паролів

## Швидке налаштування

```bash
# 1. Створити .env файл
cp .env.example .env

# 2. Відредагувати пароль
nano .env

# 3. Змінити SUDO_PASSWORD на свій пароль
SUDO_PASSWORD=Qwas@000
```

## Як це працює

### Автоматичне створення .env

При першому запуску будь-якого скрипта:
- Автоматично створюється `.env` з `.env.example`
- Використовується пароль за замовчуванням: `Qwas@000`
- Можна змінити пароль у будь-який момент

### Підтримувані скрипти

✅ **deep_windsurf_cleanup.sh** - автоматичне введення пароля  
✅ **deep_vscode_cleanup.sh** - автоматичне введення пароля  
✅ **web_interface/server.py** - передає пароль до скриптів  

### Структура .env

```bash
# Пароль для sudo команд
SUDO_PASSWORD=Qwas@000

# Порт веб-сервера
WEB_PORT=8888

# Директорії (опціонально)
CONFIGS_DIR=./configs
CONFIGS_VSCODE_DIR=./configs_vscode
HISTORY_DIR=./history
```

## Безпека

🔒 **Захист:**
- `.env` додано до `.gitignore`
- Пароль НЕ комітиться в Git
- Файл доступний тільки локально

⚠️ **Рекомендації:**
- Використовуйте сильні паролі
- Не діліться файлом `.env`
- Регулярно змінюйте паролі
- Встановіть права доступу: `chmod 600 .env`

## Приклади

### Зміна пароля

```bash
# Відкрити .env
nano .env

# Змінити рядок
SUDO_PASSWORD=НовийСильнийПароль123!

# Зберегти (Ctrl+O, Enter, Ctrl+X)
```

### Перевірка налаштувань

```bash
# Показати всі налаштування
cat .env

# Показати тільки пароль (обережно!)
grep SUDO_PASSWORD .env

# Перевірити що файл існує
ls -la .env
```

### Скидання до значень за замовчуванням

```bash
# Видалити поточний .env
rm .env

# Скопіювати з прикладу
cp .env.example .env

# Або просто перезаписати
cp -f .env.example .env
```

## Усунення проблем

### Скрипт запитує пароль

**Причина:** `.env` не існує або `SUDO_PASSWORD` не встановлено

**Рішення:**
```bash
# Створити .env
cp .env.example .env

# Перевірити вміст
cat .env | grep SUDO_PASSWORD

# Переконатись що пароль правильний
```

### Пароль не працює

**Причина:** Невірний формат або зайві символи

**Рішення:**
```bash
# Відкрити .env
nano .env

# Формат має бути:
SUDO_PASSWORD=ВашПароль

# БЕЗ лапок, БЕЗ пробілів навколо =
# Правильно:   SUDO_PASSWORD=Qwas@000
# Неправильно: SUDO_PASSWORD = "Qwas@000"
# Неправильно: SUDO_PASSWORD='Qwas@000'
```

### Веб-інтерфейс не бачить пароль

**Причина:** Сервер не перезапущено після зміни `.env`

**Рішення:**
```bash
# Зупинити сервер (Ctrl+C)

# Перезапустити
cd web_interface
python3 server.py
```

## Допоміжні файли

- **`.env.example`** - Шаблон з значеннями за замовчуванням
- **`sudo_helper.sh`** - Скрипт для автоматичного введення пароля
- **`load_env.sh`** - Функції завантаження змінних середовища

## Детальна документація

📖 [ENV_SETUP.md](ENV_SETUP.md) - Повна документація по налаштуванню  
📖 [SECURITY.md](SECURITY.md) - Рекомендації з безпеки  
📖 [README.md](README.md) - Загальна документація системи
