# 📦 Автоматична інсталяція Windsurf та VS Code

## 🎯 НОВА ФУНКЦІЯ: Автоматична інсталяція!

**Система автоматично встановлює Windsurf та VS Code після cleanup!**

---

## 🚀 Як це працює

### Для Windsurf:
1. Скрипт `deep_windsurf_cleanup.sh` виконує cleanup
2. Автоматично шукає `Windsurf-darwin-arm64-1.12.5.dmg` в репозиторії
3. Монтує DMG образ
4. Копіює Windsurf.app в `/Applications/`
5. Розмонтовує образ
6. **Готово!** Windsurf встановлено та готовий до запуску

### Для VS Code:
1. Скрипт `deep_vscode_cleanup.sh` виконує cleanup
2. Автоматично шукає `Visual Studio Code.app` в репозиторії
3. Копіює VS Code.app в `/Applications/`
4. **Готово!** VS Code встановлено та готовий до запуску

---

## 📁 Інсталятори в репозиторії

```bash
# Windsurf (DMG формат)
Windsurf-darwin-arm64-1.12.5.dmg    # 180 MB

# VS Code (APP формат)
Visual Studio Code.app/              # 645 MB
```

⚠️ **Примітка:** Файли `.dmg` та `.app/` додані до `.gitignore` для зменшення розміру репозиторію

---

## 💡 Використання

### Windsurf Cleanup з автоінсталяцією:

```bash
# 1. Закрийте Windsurf якщо запущений
killall Windsurf 2>/dev/null

# 2. Запустіть cleanup (автоматично встановить Windsurf)
./deep_windsurf_cleanup.sh

# 3. Після завершення - запустіть Windsurf
open -a Windsurf
```

### VS Code Cleanup з автоінсталяцією:

```bash
# 1. Закрийте VS Code якщо запущений
killall "Visual Studio Code" 2>/dev/null

# 2. Запустіть cleanup (автоматично встановить VS Code)
./deep_vscode_cleanup.sh

# 3. Після завершення - запустіть VS Code
open -a "Visual Studio Code"
```

---

## ✅ Що відбувається автоматично

| Крок | Windsurf | VS Code |
|------|----------|----------|
| 1. Cleanup | ✅ Автоматично | ✅ Автоматично |
| 2. Пошук інсталятора | ✅ DMG файл | ✅ APP папка |
| 3. Встановлення | ✅ Монтування + копіювання | ✅ Копіювання |
| 4. Готовність | ✅ Одразу | ✅ Одразу |
| 5. Перезавантаження | ❌ НЕ потрібне | ❌ НЕ потрібне |

---

## 🔧 Якщо інсталятор не знайдено

### Windsurf:
```bash
# Скачайте DMG файл
curl -L "https://codeium.com/windsurf/download" -o Windsurf-darwin-arm64-1.12.5.dmg

# Помістіть в репозиторій
mv Windsurf-darwin-arm64-1.12.5.dmg /path/to/System/
```

### VS Code:
```bash
# Скачайте та розпакуйте
curl -L "https://code.visualstudio.com/sha/download?build=stable&os=darwin-universal" -o VSCode.zip
unzip VSCode.zip

# Помістіть в репозиторій
mv "Visual Studio Code.app" /path/to/System/
```

---

## 📖 Детальна документація

- [README.md](README.md) - Загальна документація
- [ENV_SETUP.md](ENV_SETUP.md) - Налаштування паролів
- [PASSWORD_CONFIG.md](PASSWORD_CONFIG.md) - Конфігурація sudo
- [WEB_INTERFACE_README.md](WEB_INTERFACE_README.md) - Веб-інтерфейс
