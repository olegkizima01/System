# 🌐 Web Interface - Hacker Dashboard

Веб-інтерфейс для управління Deep Cleanup System в хакерському стилі.

## 🚀 Запуск

### Метод 1: Через головне меню
```bash
./main_menu.sh
# Оберіть [3] Web Interface
```

### Метод 2: Прямий запуск
```bash
cd web_interface
python3 server.py
```

### Метод 3: Через аліас
```bash
# Додайте до ~/.zshrc:
alias web-cleanup='cd ~/Documents/GitHub/System/web_interface && python3 server.py'

# Використання:
web-cleanup
```

## 📱 Доступ

Після запуску відкрийте в браузері:
```
http://localhost:8888
```

## ✨ Функції

### 📊 System Status
- **Real-time monitoring** - Оновлення кожні 5 секунд
- **Hostname** - Поточний hostname системи
- **Windsurf Status** - Статус встановлення
- **VS Code Status** - Статус встановлення
- **Profiles Count** - Кількість збережених профілів

### 🌊 Windsurf System
- **Full Cleanup + Auto Install** - Повне очищення з автоматичною інсталяцією
- **Manage Profiles** - Управління профілями
- **Check Status** - Перевірка статусу
- **Restore** - Відновлення з профілю

### 💻 VS Code System
- **Full Cleanup** - Повне очищення
- **Manage Profiles** - Управління профілями
- **Check Status** - Перевірка статусу
- **Restore** - Відновлення з профілю

### 📜 Change History
- Відображення всіх змін
- Timestamp кожної операції
- Фільтрація по системах
- Експорт історії

### 💻 Terminal Output
- Real-time логи
- Кольорове виділення (success/error/warning)
- Автоматичне прокручування
- Збереження останніх 50 рядків

## 🎨 Дизайн

### Хакерський стиль:
- **Matrix Background** - Анімований фон в стилі Matrix
- **Neon Colors** - Неонові кольори (#00ff41, #00d4ff)
- **Monospace Font** - Courier New для автентичності
- **Glowing Effects** - Світіння елементів
- **Animations** - Плавні анімації та переходи

### Кольорова схема:
```css
--bg-primary: #0a0e27      /* Темний фон */
--text-primary: #00ff41    /* Зелений неон */
--text-secondary: #00d4ff  /* Синій неон */
--danger: #ff0055          /* Червоний */
--warning: #ffaa00         /* Помаранчевий */
```

## 🔧 API Endpoints

### GET Endpoints:
```
GET /api/status              - Статус системи
GET /api/configs/windsurf    - Профілі Windsurf
GET /api/configs/vscode      - Профілі VS Code
GET /api/history             - Історія змін
```

### POST Endpoints:
```
POST /api/cleanup/windsurf   - Запуск cleanup Windsurf
POST /api/cleanup/vscode     - Запуск cleanup VS Code
POST /api/restore/windsurf   - Відновлення Windsurf
POST /api/restore/vscode     - Відновлення VS Code
```

## 📁 Структура

```
web_interface/
├── server.py              # Python веб-сервер
├── templates/
│   └── index.html        # Головна сторінка
└── static/
    ├── style.css         # Стилі (хакерський дизайн)
    └── script.js         # JavaScript логіка
```

## 🔒 Безпека

### Рекомендації:
1. **Локальний доступ** - Сервер працює тільки на localhost
2. **Не відкривайте порт** - Не робіть port forwarding
3. **Firewall** - Переконайтеся що firewall активний
4. **Пароль sudo** - Операції потребують sudo пароль

### Обмеження:
- Доступ тільки з localhost (127.0.0.1)
- Порт 8888 (можна змінити в server.py)
- Без автентифікації (локальний доступ)

## 🎯 Використання

### Сценарій 1: Cleanup Windsurf
1. Відкрийте веб-інтерфейс
2. Перейдіть в секцію "Windsurf System"
3. Натисніть "Full Cleanup + Install"
4. Підтвердіть операцію
5. Спостерігайте за процесом в Terminal Output
6. Перевірте результат в System Status

### Сценарій 2: Перемикання профілів
1. Натисніть "Manage Profiles"
2. Оберіть потрібний профіль
3. Натисніть "Restore"
4. Підтвердіть операцію
5. Система автоматично відновить профіль

### Сценарій 3: Моніторинг
1. Залиште веб-інтерфейс відкритим
2. Статус оновлюється кожні 5 секунд
3. Всі зміни відображаються в History
4. Terminal показує real-time логи

## 🐛 Troubleshooting

### Проблема: Порт зайнятий
```bash
# Знайти процес на порту 8888
lsof -i :8888

# Вбити процес
kill -9 <PID>

# Або змінити порт в server.py
PORT = 9999  # Інший порт
```

### Проблема: Python не знайдено
```bash
# Встановити Python 3
brew install python3

# Або використати system Python
python3 --version
```

### Проблема: Не завантажується статус
```bash
# Перевірити що скрипти executable
chmod +x ../deep_windsurf_cleanup.sh
chmod +x ../deep_vscode_cleanup.sh
chmod +x ../history_tracker.sh

# Перевірити шляхи в server.py
```

## 📊 Статистика

### Розмір файлів:
- `server.py` - ~5KB (Python backend)
- `index.html` - ~6KB (HTML структура)
- `style.css` - ~10KB (Хакерський дизайн)
- `script.js` - ~8KB (JavaScript логіка)

### Функціонал:
- 8 API endpoints
- 15+ JavaScript функцій
- 50+ CSS анімацій
- Real-time оновлення

## 🎨 Кастомізація

### Зміна кольорів:
Відредагуйте `static/style.css`:
```css
:root {
    --text-primary: #ff00ff;    /* Ваш колір */
    --text-secondary: #00ffff;  /* Ваш колір */
}
```

### Зміна порту:
Відредагуйте `server.py`:
```python
PORT = 9999  # Ваш порт
```

### Зміна інтервалу оновлення:
Відредагуйте `static/script.js`:
```javascript
setInterval(loadSystemStatus, 10000);  // 10 секунд
```

## 🚀 Майбутні покращення

### Заплановано:
- [ ] Автентифікація користувачів
- [ ] HTTPS підтримка
- [ ] Графіки та статистика
- [ ] Експорт звітів (PDF, CSV)
- [ ] Мобільна версія
- [ ] Dark/Light theme toggle
- [ ] Notifications (WebSocket)
- [ ] Multi-language support

## 📚 Технології

- **Backend**: Python 3 (http.server)
- **Frontend**: Vanilla JavaScript (ES6+)
- **Styling**: CSS3 (Animations, Grid, Flexbox)
- **Icons**: Unicode Emoji
- **API**: RESTful JSON

## 🎓 Навчання

### Для розробників:
1. Вивчіть `server.py` - Python backend
2. Вивчіть `script.js` - JavaScript frontend
3. Вивчіть `style.css` - CSS анімації
4. Експериментуйте з API endpoints

### Для користувачів:
1. Запустіть веб-інтерфейс
2. Спробуйте всі функції
3. Перегляньте історію змін
4. Експериментуйте з профілями

## 🔗 Посилання

- [Main README](../README.md)
- [VS Code README](../README_VSCODE.md)
- [Security Guide](../SECURITY.md)
- [Changelog V3](../CHANGELOG_V3.md)

## 💡 Поради

1. **Тримайте веб-інтерфейс відкритим** - Для моніторингу
2. **Використовуйте Terminal Output** - Для debugging
3. **Перевіряйте History** - Перед restore
4. **Робіть бекапи** - Перед cleanup
5. **Читайте логи** - Для розуміння процесів

---

**Версія**: 1.0  
**Дата**: 2025  
**Стиль**: Hacker Theme  
**Статус**: ✅ Production Ready
