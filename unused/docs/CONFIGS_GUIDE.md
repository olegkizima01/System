# 🔧 Система управління конфігураціями Windsurf

## 📋 Що це?

Система управління конфігураціями дозволяє зберігати та перемикатися між різними профілями Windsurf ідентифікаторів. Кожна конфігурація - це набір унікальних ідентифікаторів, які Windsurf використовує для розпізнавання клієнта.

---

## 🎯 Основні концепції

### 1. Оригінальна конфігурація (`configs/original`)
- **Зберігається автоматично** при першому запуску cleanup скрипта
- **Використовується для автовідновлення** через 5 годин
- **Містить ваші справжні ідентифікатори**
- Можна перезаписати через manage_configs.sh

### 2. Автоматичні конфігурації (`profile_YYYYMMDD_HHMMSS`)
- **Створюються автоматично** при кожному запуску cleanup скрипта
- **Містять нові згенеровані ідентифікатори:**
  - Machine-ID: криптографічно випадковий hex (64 символи)
  - Device-ID: випадковий UUID
  - Hostname: **унікальний випадковий** (формат: `Mac-A7F2E4B9`)
- Зберігаються в `configs/`
- Можна відновити вручну в будь-який момент

### 3. Власні конфігурації
- **Створюються вручну** через manage_configs.sh
- Можна зберегти поточний стан під будь-якою назвою
- Зручно для управління кількома "клієнтами"

---

## 🚀 Використання

### Запустити менеджер конфігурацій

```bash
./manage_configs.sh
```

Або через головне меню:
```bash
./start.sh
# Виберіть опцію 8
```

---

## 📚 Функції менеджера

### 1️⃣ Зберегти поточну конфігурацію

Зберігає поточні ідентифікатори Windsurf під вказаною назвою.

**Що зберігається:**
- Machine-ID
- Device-ID (storage.json)
- Global Storage
- Hostname

**Приклад:**
```
Введіть назву: client_work
✅ Конфігурація 'client_work' збережена
```

### 2️⃣ Відновити конфігурацію

Відновлює збережену конфігурацію.

**Що відбувається:**
- Замінює поточний Machine-ID
- Замінює Device-ID
- Змінює hostname
- Очищає DNS кеш

**Приклад:**
```
Введіть назву: original
✅ Конфігурація 'original' відновлена
```

### 3️⃣ Список конфігурацій

Показує всі збережені конфігурації з деталями.

**Виводить:**
- Назва конфігурації
- Дата створення
- Hostname
- Що збережено (Machine-ID, Storage, etc.)

### 4️⃣ Видалити конфігурацію

Видаляє збережену конфігурацію (крім `original` - попросить підтвердження).

### 5️⃣ Зберегти як ОРИГІНАЛ

Зберігає поточну конфігурацію як `original` (для автовідновлення).

**Коли використовувати:**
- Змінили справжні ідентифікатори
- Хочете змінити що відновлюється через 5 годин
- Переустановили Windsurf

---

## 📖 Приклади використання

### Сценарій 1: Робота з двома клієнтами

```bash
# 1. Зберегти поточну конфігурацію (клієнт 1)
./manage_configs.sh
# Вибрати 1 → ввести "client1"

# 2. Запустити cleanup для створення нового профілю
./deep_windsurf_cleanup.sh
# Автоматично створюється profile_20251015_203000

# 3. Працювати з Windsurf як клієнт 2
open -a Windsurf

# 4. Повернутися до клієнта 1
./manage_configs.sh
# Вибрати 2 → ввести "client1"

# 5. Перезапустити Windsurf
killall Windsurf && sleep 2 && open -a Windsurf
```

### Сценарій 2: Тестування різних профілів

```bash
# Створити кілька тестових профілів
for i in {1..3}; do
    ./deep_windsurf_cleanup.sh
    sleep 10
    ./manage_configs.sh
    # Вибрати 1 → ввести "test$i"
done

# Перемикатися між ними
./manage_configs.sh
# Вибрати 2 → ввести "test1" або "test2" або "test3"
```

### Сценарій 3: Збереження оригіналу перед експериментами

```bash
# 1. Зберегти поточний стан як оригінал
./manage_configs.sh
# Вибрати 5 (Зберегти як ОРИГІНАЛ)

# 2. Експериментувати скільки завгодно
./deep_windsurf_cleanup.sh  # Створить новий профіль

# 3. Через 5 годин автоматично повернеться до оригіналу
# АБО відновити вручну:
./manage_configs.sh
# Вибрати 2 → ввести "original"
```

---

## 📂 Структура конфігурації

Кожна конфігурація зберігається в окремій папці:

```
configs/
├── original/                    # Оригінальна конфігурація
│   ├── machineid               # Machine-ID
│   ├── storage.json            # Device-ID та інше
│   ├── User/
│   │   └── globalStorage/
│   │       └── storage.json    # Global storage
│   ├── hostname.txt            # Hostname
│   └── metadata.json           # Метадані
│
├── profile_20251015_203000/    # Автоматична конфігурація
│   ├── machineid
│   ├── storage.json
│   ├── User/globalStorage/storage.json
│   ├── hostname.txt
│   └── metadata.json
│
└── client_work/                 # Власна конфігурація
    ├── machineid
    ├── storage.json
    ├── User/globalStorage/storage.json
    ├── hostname.txt
    └── metadata.json
```

---

## ⚠️ Важливо

### Що НЕ зберігається
- Розширення Windsurf
- Налаштування редактора
- Історія файлів
- Кеші

Зберігаються **тільки ідентифікатори** для розпізнавання клієнта.

### Gitignore
Папка `configs/` додана в `.gitignore`, тому:
- ✅ Конфігурації НЕ потраплять в Git
- ✅ Ваші ідентифікатори залишаться приватними
- ❌ Конфігурації НЕ синхронізуються між машинами

### Перезавантаження
- Конфігурації в `configs/` **НЕ видаляються** при перезавантаженні
- Це не `/tmp`, тому безпечно
- Зберігаються назавжди (поки не видалите вручну)

---

## 🎓 FAQ

**Q: Скільки конфігурацій можна зберігати?**  
A: Необмежену кількість. Обмежується тільки місцем на диску (кожна ~1-5KB).

**Q: Чи можна експортувати конфігурацію?**  
A: Так, просто скопіюйте папку з `configs/` в інше місце:
```bash
cp -r configs/client1 ~/Desktop/
```

**Q: Чи можна імпортувати конфігурацію?**  
A: Так, скопіюйте папку в `configs/`:
```bash
cp -r ~/Desktop/client1 configs/
```

**Q: Що станеться якщо видалити `original`?**  
A: Автовідновлення через 5 годин не спрацює. Скрипт попросить підтвердження.

**Q: Чи змінюється поточна конфігурація Windsurf при cleanup?**  
A: Так, cleanup замінює поточні ідентифікатори на нові. Старі зберігаються в `configs/`.

**Q: Чи потрібен Windsurf для роботи з конфігураціями?**  
A: Ні, можна зберігати та відновлювати конфігурації без встановленого Windsurf.

---

## 💡 Поради

1. **Завжди зберігайте оригінал** перед експериментами
2. **Називайте конфігурації зрозуміло** (client1, work, personal, test)
3. **Видаляйте старі профілі** щоб не захаращувати
4. **Робіть бекап configs/** перед видаленням скриптів
5. **Закривайте Windsurf** перед зміною конфігурації

---

**Успішного управління профілями!** 🎉
