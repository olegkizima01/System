# 🚀 VS Code Deep Cleanup System

Система скриптів для повного очищення VS Code з можливістю автоматичного відновлення оригінальних ідентифікаторів.

## 📋 Склад системи

### 1. `deep_vscode_cleanup.sh` - Основний скрипт очищення
Виконує глибоке очищення всіх слідів VS Code і підміняє ідентифікатори на нові.

**Що робить:**
- ✅ **ВИДАЛЯЄ додаток VS Code з /Applications** (потрібна переінсталяція)
- ✅ Видаляє всі файли та папки VS Code
- ✅ Очищує Keychain від всіх записів VS Code, GitHub, Microsoft
- ✅ **Створює бекап** оригінальних `machine-id` та `device-id`
- ✅ **Підміняє ідентифікатори** на нові (криптографічно випадкові)
- ✅ Змінює hostname на **випадковий** з 5 різних форматів (150+ імен)
- ✅ Очищує DNS кеш
- ✅ **Автоматично відновлює** все через 5 годин
- ✅ **Зберігає конфігурації** в `configs_vscode/` (оригінал + кожен новий профіль)

### 2. `restore_vscode_backup.sh` - Ручне відновлення
Дозволяє відновити оригінальні ідентифікатори вручну (до 5 годин).

### 3. `check_vscode_backup.sh` - Перевірка статусу
Показує інформацію про створені бекапи та процес автовідновлення.

### 4. `manage_vscode_configs.sh` - Управління конфігураціями
Інтерактивне меню для перемикання між збереженими профілями VS Code.

---

## 🎯 Використання

### Базовий сценарій (з автовідновленням)

```bash
# 1. Закрийте VS Code якщо він запущений
# (Це важливо для коректного очищення файлів)

# 2. Запустити очищення
./deep_vscode_cleanup.sh

# 3. Зачекайте 5-10 секунд для завершення всіх операцій

# 4. ПЕРЕІНСТАЛЮЙТЕ VS Code (він був видалений на кроці 2)
# Скачайте з: https://code.visualstudio.com/
# Встановіть в /Applications/

# 5. ⚠️ ПЕРЕЗАВАНТАЖЕННЯ НЕ ПОТРІБНЕ!
# VS Code можна запускати ОДРАЗУ після встановлення

# 6. VS Code побачить вас як НОВОГО користувача
# При першому запуску може попросити авторизацію
# Hostname буде УНІКАЛЬНИЙ у одному з 5 форматів:
#    • Name-Place (Alex-Studio)
#    • Name-Place-Suffix (James-MacBook-Pro)
#    • Prefix-Name (Work-Michael)
#    • Name's-Place (Emma-iMac)
#    • Place-Name (MacBook-Sarah)

# 7. Працюйте до 5 годин

# 8. Через 5 годин автоматично відновляться:
#    - Оригінальний hostname
#    - Оригінальний machine-id
#    - Оригінальний device-id
```

### ❓ Чи потрібне перезавантаження?

**НІ! Перезавантаження НЕ потрібне.**

✅ **Можна одразу:**
- Встановити VS Code (якщо видалили)
- Запустити VS Code
- Авторизуватися як новий користувач

❌ **Перезавантаження:**
- Не потрібне для роботи VS Code
- Видалить бекапи з /tmp
- Зупинить процес автовідновлення

💡 **Рекомендація:** 
Якщо плануєте перезавантаження - спочатку скопіюйте бекапи або відновіть їх вручну.

### Перевірка статусу бекапів

```bash
./check_vscode_backup.sh
```

**Виведе:**
- Список всіх бекапів
- Розмір і дату створення
- Вміст бекапів
- Статус процесу автовідновлення
- Збережені конфігурації

### Ручне відновлення (раніше 5 годин)

```bash
./restore_vscode_backup.sh
```

**Коли використовувати:**
- Потрібно відновити раніше 5 годин
- Процес автовідновлення був припинений
- Хочете повернутися до оригінального клієнта

### Управління конфігураціями

```bash
./manage_vscode_configs.sh
```

**Функції:**
- Перегляд всіх збережених профілів
- Перемикання між профілями
- Видалення непотрібних конфігурацій
- Перегляд поточного стану системи

---

## 🔍 Детальна інформація

### 🎲 Формати Hostname (5 варіантів)

Скрипт випадково обирає один з 5 форматів для максимальної реалістичності:

1. **Name-Place**: `Alex-Studio`, `Emma-Desktop`
2. **Name-Place-Suffix**: `James-MacBook-Pro`, `Sarah-iMac-Max`
3. **Prefix-Name**: `Work-Michael`, `Home-Olivia`
4. **Name's-Place**: `Alex-MacBook`, `Emma-iMac`
5. **Place-Name**: `MacBook-Alex`, `Studio-James`

**150+ реальних імен:**
- Чоловічі: Alex, James, Michael, David, Robert, John, Richard, Charles, Daniel, Matthew та інші
- Жіночі: Emma, Olivia, Ava, Sophia, Isabella, Mia, Charlotte, Amelia, Harper, Evelyn та інші

**50+ назв пристроїв:**
Studio, Office, Desktop, Workspace, iMac, MacBook, MacStudio, MacPro, Mini, Pro, Air, MBP, MBA, Mac, Laptop, Tower, Rig, Setup, Build, Dev, Work, Home та інші

### 🔐 Де зберігаються токени VS Code

Токени та ідентифікатори зберігаються в наступних місцях:

#### 1. **Основна база даних (КРИТИЧНО)**
```bash
~/Library/Application Support/Code/User/globalStorage/state.vscdb
~/Library/Application Support/Code/User/globalStorage/state.vscdb.backup
```
- SQLite бази даних з **ВСІМА** токенами
- GitHub, Microsoft, Azure токени
- **Скрипт видаляє ці файли повністю!**

#### 2. **Локальні сховища**
```bash
~/Library/Application Support/Code/Local Storage
~/Library/Application Support/Code/Session Storage  
~/Library/Application Support/Code/IndexedDB
```

#### 3. **Keychain (macOS)**
- Записи: `Visual Studio Code`, `vscode`, `github.com`, `microsoft.com`
- OAuth credentials, Personal Access Tokens
- **Скрипт очищує всі ці записи!**

#### 4. **Файли конфігурації**
```bash
~/Library/Application Support/Code/storage.json
~/Library/Application Support/Code/User/globalStorage/storage.json
```

**✅ Скрипт `deep_vscode_cleanup.sh` видаляє ВСІ ці файли та записи!**

### Що зберігається в бекапі?

**Директорія:** `/tmp/vscode_backup_<timestamp>/`

**Файли:**
1. `machineid.bak` - Оригінальний Machine-ID
2. `storage.json.bak` - Оригінальні Device-ID та Session-ID
3. `User_globalStorage_storage.json.bak` - Глобальні налаштування

⚠️ **ВАЖЛИВО:** Токени GitHub/Microsoft НЕ зберігаються в бекапі! Вони видаляються повністю.

### Що підміняється на нові значення?

```json
{
  "telemetry.machineId": "новий-випадковий-hex-64-символи",
  "telemetry.macMachineId": "новий-випадковий-hex-64-символи",
  "telemetry.devDeviceId": "новий-uuid",
  "telemetry.sqmId": "{новий-uuid}",
  "install.time": "поточний-timestamp",
  "sessionId": "новий-uuid"
}
```

### Чому підміна краща за видалення?

✅ **Підміна:**
- VS Code бачить валідні ідентифікатори (новий клієнт)
- Можливість повернутися до старого облікового запису
- Зберігає структуру файлів
- Немає підозрілих порожніх полів

❌ **Видалення:**
- VS Code може підозрювати маніпуляції
- Неможливо відновити оригінального клієнта
- Може викликати помилки при першому запуску

---

## ⚠️ Важливі моменти

### Автовідновлення НЕ спрацює якщо:

1. **Перезавантажити Mac**
   - Процес у фоні завершиться
   - Бекапи в `/tmp` видаляться
   - **Рішення:** Не перезавантажуйте або використайте ручне відновлення ДО перезавантаження

2. **Вимкнути Mac**
   - Аналогічно перезавантаженню
   - **Рішення:** Тримайте Mac увімкненим 5 годин

3. **Вручну вбити процес**
   - Якщо виконати `kill <PID>`
   - **Рішення:** Не вбивайте процес або використайте ручне відновлення

### Як зберегти бекапи назавжди?

```bash
# Знайти директорію бекапу
ls -ld /tmp/vscode_backup_*

# Скопіювати в безпечне місце
cp -r /tmp/vscode_backup_XXXXXXXXXX ~/Documents/vscode_backup_permanent

# Відновити пізніше
cp ~/Documents/vscode_backup_permanent/machineid.bak ~/Library/Application\ Support/Code/machineid
# ... і так для всіх файлів
```

---

## 🛠️ Технічні деталі

### Як VS Code ідентифікує клієнта?

1. **Machine-ID** (`machineid`)
   - 64-символьний hex рядок
   - Унікальний для кожної інсталяції
   - Зберігається в `~/Library/Application Support/Code/`

2. **Device-ID** (`storage.json`)
   - UUID формат
   - Зв'язує device з обліковим записом
   - Зберігається в telemetry налаштуваннях

3. **Hostname**
   - Системне ім'я Mac
   - 5 різних форматів для різноманітності
   - Змінюється через `scutil`

4. **Keychain записи**
   - Токени автентифікації GitHub, Microsoft
   - OAuth credentials
   - Видаляються через `security` команди

5. **Storage файли**
   - Session ID
   - Install time
   - SQM ID (Software Quality Metrics)

### Що генерується скриптом?

```bash
# Machine-ID (hex, 64 символи)
openssl rand -hex 32

# Device-ID, Session-ID, SQM-ID (UUID)
uuidgen | tr '[:upper:]' '[:lower:]'

# Timestamp
date +%s

# Hostname (один з 5 форматів)
# Випадково обирається формат та імена з масивів
```

---

## 📊 Приклад виведення

### deep_vscode_cleanup.sh
```
==================================================
🚀 ГЛИБОКЕ ВИДАЛЕННЯ VS CODE ДЛЯ НОВОГО КЛІЄНТА
==================================================

[1/12] Видалення VS Code папок...
🗑️  Видаляю: /Users/dev/Library/Application Support/Code

[8/12] Резервування та підміна ідентифікаторів...
📦 Бекап: /tmp/vscode_backup_1729020420
✅ Machine-ID підмінено
✅ Storage підмінено
...

💾 Бекап: /tmp/vscode_backup_1729020420
📂 Конфігурація: configs_vscode/Emma-MacBook-Pro
⏰ Автовідновлення через 5 годин (PID: 12345)

💡 ВСТАНОВЛЕННЯ VS CODE:
   • Скачайте: https://code.visualstudio.com/
   • Перезавантаження НЕ потрібне!
```

### check_vscode_backup.sh
```
📊 ІНФОРМАЦІЯ ПРО VS CODE БЕКАПИ
📁 Знайдено бекапів: 1

📦 Бекап #vscode_backup_1729020420
📅 Дата створення: 15.10.2025 20:07:00
💾 Розмір: 24K
   ✓ machineid.bak (64B)
   ✓ storage файлів: 2 шт.

⏰ Процес автовідновлення:
✓ Процес активний (PID: 12345)

📂 Збережені конфігурації:
📁 Знайдено конфігурацій: 3
   • original (DEVs-Mac-Studio)
   • Emma-MacBook-Pro
   • Work-Michael
```

---

## 🎓 FAQ

**Q: Чи безпечно використовувати ці скрипти?**  
A: Так, всі оригінальні дані зберігаються в бекапі. Ви завжди можете відновити.

**Q: Чи можна використовувати кілька разів?**  
A: Так, кожен запуск створить новий профіль з унікальним hostname.

**Q: Що якщо забув про автовідновлення і перезавантажив Mac?**  
A: Бекапи в `/tmp` видаляться, але збережені конфігурації залишаться в `configs_vscode/`.

**Q: Чи можна змінити час відновлення (не 5 годин)?**  
A: Так, відредагуйте `sleep 18000` в скрипті (18000 сек = 5 год).

**Q: VS Code все одно розпізнає мене. Що робити?**  
A: Перевірте Keychain (можливо залишились записи GitHub/Microsoft), та системні логи.

**Q: Чи можна мати кілька профілів?**  
A: Так! Кожен cleanup створює новий профіль. Перемикайтеся через `manage_vscode_configs.sh`.

**Q: Чи втрачу я налаштування VS Code?**  
A: Так, всі налаштування та розширення будуть видалені. Зробіть бекап якщо потрібно.

---

## 🔗 Порівняння з Windsurf

| Функція | Windsurf | VS Code |
|---------|----------|---------|
| Видалення додатку | ✅ | ✅ |
| Підміна Machine-ID | ✅ | ✅ |
| Очищення Keychain | ✅ Windsurf, Codeium | ✅ VS Code, GitHub, Microsoft |
| Формати hostname | 5 варіантів | 5 варіантів |
| Кількість імен | 150+ | 150+ |
| Автовідновлення | 5 годин | 5 годин |
| Управління профілями | ✅ | ✅ |

---

## 📝 Ліцензія

Використовуйте на свій розсуд. Автор не несе відповідальності за втрату даних.

**Створено:** 2025  
**Версія:** 1.0 (з автовідновленням та 5 форматами hostname)
