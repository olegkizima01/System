Передмова:
Це адаптоване технічне завдання (ТЗ), що розширює можливості системи до універсального агента з виділеною підсистемою для роботи з кодом та двома режимами виконання.

Формат — офіційне ТЗ, яке можна:
	•	передати іншому інженеру
	•	використовувати як основу для реалізації
	•	зберегти як MCP/architecture spec

⸻

ТЕХНІЧНЕ ЗАВДАННЯ

Універсальна Агентна Система з Каскадним Керуванням Windsurf (Dev Subsystem)

⸻

1. Мета системи

Реалізувати універсальну агентну систему, здатну виконувати широкий спектр завдань (рівень людини та вище).

1.1 Універсальність
Система не є вузькоспеціалізованою. Вона повинна обробляти будь-які запити користувача (робота з ОС, браузером, документами, медіа тощо).

1.2 Спеціалізація на коді (Dev Subsystem)
Якщо завдання стосується розробки, зміни коду, аналізу коду або має будь-який натяк на програмування, воно передається в окремий контур — **Підсистему Розробки (Dev Subsystem)**. Це необхідно для забезпечення якості та контролю змін у кодовій базі.

Підсистема розробки реалізує каскадну агентну архітектуру, у якій:
	•	кастомний LLM-агент **Copilot** (GPT-4o) виконує роль мета-контролера поведінки;
	•	**Continue CLI** використовується як **драйвер** (шина взаємодії та інструментальний рівень);
	•	**Windsurf IDE** (модель GPT-5.2 Medium Reasoning) працює у стандартному режимі, ніби з ним взаємодіє користувач;
	•	результат взаємодії Copilot ↔ Windsurf за поведінкою та глибиною має бути еквівалентний прямій взаємодії користувача з Windsurf.

⸻

2. Архітектура Підсистеми Розробки (Dev Subsystem)

Підсистема має **два режими роботи**. Вибір режиму визначається автоматично на основі доступності та стабільності Windsurf IDE.

2.1 Режим 1 (Основний): Каскад через Windsurf
**Потік:** Copilot → Continue CLI (Driver) → Windsurf IDE

*   **Continue CLI як Драйвер:** Continue CLI є транспортним шаром. Він не приймає рішень, а лише виконує команди Copilot.
*   **Роль Copilot:** Формує запити до Windsurf у людському стилі та відправляє їх **у чат Windsurf** через драйвер (Continue CLI). Аналізує відповіді, керує циклом діалогу та stop-умовами.
*   **Роль Windsurf:** Аналізує кодову базу, генерує відповіді/код, вносить зміни у файли. Windsurf не знає про існування Copilot і вважає, що з ним спілкується користувач.

2.2 Режим 2 (Fallback): Нативний Continue CLI
**Потік:** Copilot → Continue CLI (Native Execution)

*   **Умова активації:** Якщо Continue CLI (як драйвер) виявляє, що взаємодія з Windsurf неможлива (блокування UI, відсутність реакції, технічний збій).
*   **Логіка:** Continue CLI перестає намагатися керувати Windsurf і перемикається на **власні вбудовані інструменти**.
*   **Можливості (повні):**
    *   Читання файлів.
    *   Пошук по кодовій базі.
    *   Аналіз коду.
    *   **Редагування та написання коду** (використовуючи можливості Continue для edit/apply).
*   **Ціль:** Забезпечити виконання задачі навіть за відсутності Windsurf, використовуючи Continue як повноцінне резервне середовище.

⸻

3. Компоненти системи та Ролі (MCP-style контракт)

3.1 Router (Маршрутизатор)
*   Аналізує вхідний запит користувача.
*   Якщо запит стосується коду/розробки → передає управління **Dev Subsystem (Copilot)**.
*   Якщо запит загальний (ОС, браузер, медіа) → передає управління **General Agent**.

3.2 Copilot (Meta-Agent / "Brain")

Обов'язки:
	•	формувати запити до Windsurf у людському стилі
	•	відправляти повідомлення в чат Windsurf через Continue CLI
	•	аналізувати відповіді Windsurf
	•	визначати: достатність відповіді, необхідність уточнення, зміну напряму
	•	підтримувати довгострокову мету діалогу
	•	керувати stop-умовами
	•	приймати рішення про перехід у Fallback-режим

Обмеження:
	•	не має прямого доступу до файлової системи (в Режимі 1)
	•	не виконує shell-команди напряму
	•	не втручається у внутрішній reasoning Windsurf

3.3 Continue CLI (The Driver / Fallback Executor)

Режим 1 (Driver):
	•	приймати запити від Copilot
	•	**передавати їх у чат Windsurf як користувацький ввід**
	•	отримувати відповіді Windsurf
	•	повертати відповіді Copilot без інтерпретації
	•	виконувати дії лише за явною командою Copilot

Режим 2 (Native/Fallback):
	•	Виконувати повний цикл розробки самостійно (читання, аналіз, редагування коду).

Обмеження (загальні):
	•	не приймає стратегічних рішень
	•	не змінює зміст повідомлень від Copilot
	•	не має власного LLM-reasoning (вся логіка — від Copilot)

3.4 Windsurf IDE (Reasoning & Execution Core)

Обов'язки:
	•	аналізувати кодову базу
	•	відповідати у стандартному чат-форматі
	•	використовувати свою reasoning-модель
	•	пропонувати рішення та пояснення
	•	вносити зміни у код

Обмеження:
	•	не знає про існування Copilot
	•	не знає про каскад
	•	не ініціює діалог

⸻

4. LLM-провайдери та моделі

4.1 Налаштування Continue
	•	Anthropic Claude Sonnet 4.5 → вимкнений або не використовується
	•	**Copilot GPT-4o** → єдиний LLM-провайдер Continue

Принцип:
Continue не має власної "волі" — вся поведінка визначається Copilot.

⸻

5. Потік взаємодії (Interaction Flow)

5.1 Для Dev-задач (Режим 1 — Windsurf):

1.	Copilot формує запит (як користувач Windsurf).
2.	Continue CLI (Driver) передає запит у чат Windsurf.
3.	Windsurf: аналізує, генерує відповідь/код.
4.	Continue CLI повертає відповідь Copilot.
5.	Copilot: аналізує результат, приймає рішення (продовжити / уточнити / змінити напрям / завершити).
6.	Цикл повторюється до виконання stop-умови.

5.2 Для Dev-задач (Режим 2 — Fallback):

1.	Активація: Copilot виявляє проблему з Windsurf.
2.	Continue CLI перемикається на нативний режим.
3.	Copilot формує запит напряму до Continue CLI.
4.	Continue CLI виконує задачу (читання, аналіз, редагування).
5.	Continue CLI повертає результат Copilot.
6.	Цикл повторюється до виконання stop-умови.

⸻

6. Поведінкові вимоги
	•	Поведінка системи визначається тільки промптом Copilot.
	•	Windsurf завжди працює у стандартному режимі.
	•	Copilot поводиться як: критичний співрозмовник, системний архітектор, носій стилю користувача.

Ціль:
Створити ефект, що Windsurf спілкується з живим користувачем, а не з агентом.

⸻

7. Stop-умови (обов'язково)

Цикл взаємодії завершується, якщо виконується хоча б одна умова:
	•	мета діалогу досягнута
	•	Copilot визнає відповідь достатньою
	•	відсутній новий інформаційний приріст
	•	досягнуто ліміт ітерацій
	•	Copilot ініціює завершення

**Тільки Copilot має право завершити цикл.**

⸻

8. Нефункціональні вимоги
	•	Модульність (можливість заміни моделей).
	•	Відсутність tight-coupling між агентами.
	•	Логування всіх кроків Continue CLI.
	•	Повторюваність поведінки Copilot.
	•	Автономний перехід між режимами (Windsurf ↔ Fallback) без участі користувача.

⸻

9. Очікуваний результат
	•	Copilot керує Windsurf каскадно (Режим 1).
	•	Continue виступає як драйвер (Режим 1) або повноцінний виконавець (Режим 2).
	•	Windsurf демонструє глибокий reasoning.
	•	Загальна поведінка системи: еквівалентна прямій роботі користувача з Windsurf.
	•	Система стійка до збоїв Windsurf завдяки Fallback-режиму.

⸻

10. Критерій успіху

Система вважається реалізованою успішно, якщо:
1.  Взаємодія Copilot ↔ Windsurf дає той самий стиль, глибину та контроль, що й ручна взаємодія користувача з Windsurf.
2.  При збоях Windsurf система автоматично переходить на Continue CLI (Fallback) і продовжує виконання задачі без втрати прогресу.

⸻

Наступні логічні кроки:
	•	✍️ системний промпт Copilot (v1)
	•	🧩 MCP-контракт у JSON/YAML
	•	🔧 мапінг на реальний Continue config
	•	🧠 state-machine для loop/stop та переключення режимів